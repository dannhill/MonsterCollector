class_name Typ

# I gave it this name because apparently you can't reference an anonymous enum, so I had to give it a name
# In addition, I had to use the class_name keyword to make the enum global, or at least accessible from other files

enum e{
  SLIME,
  GOBLIN,
  ORC,
  SKELETON,
  TROLL,
  KNIGHT
}
